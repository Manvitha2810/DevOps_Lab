apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-app
spec:
  selector:
    matchLabels:
      app: resume
  template:
    metadata:
      labels:
        app: resume
    spec:
      containers:
      - name: resume
        image: yourname/resume-nginx:latest
        ports:
        - containerPort: 80
